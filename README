# Mitsuba dependencies for windows

Successfully builds Mitsuba 0.6, tested on Windows 10/11.

### Additional operations:

Copy this folder to `mitsuba/dependencies`.

Copy `./dist/platforms` to `mitsuba/dist/platforms`.